Small tool to switch between php version.
How to use:
- pull localy
- update 'possibleVersions' variable values to fit your local php version
- go build -o <name>
- sudo <name> <phpversion_to_enable>
